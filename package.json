{
	"name": "sayit-hono",
	"version": "0.0.0",
	"type": "module",
	"private": true,
	"packageManager": "bun@1.3.14",
	"scripts": {
		"deploy": "echo 'BLOCKED on techdebt/vp-lemmascript-migration: prod deploy disabled during migration. Use: bun run deploy:staging' && exit 1",
		"deploy:assets": "echo 'BLOCKED on techdebt/vp-lemmascript-migration: prod deploy disabled during migration. Use: bun run deploy:staging:assets' && exit 1",
		"deploy:search": "echo 'BLOCKED on techdebt/vp-lemmascript-migration: prod deploy disabled during migration. Use: bun run deploy:staging' && exit 1",
		"deploy:staging": "bun run build:cache-version && CLOUDFLARE_ENV=staging vite build && SEARCH_R2_BUCKETS=sayit-speech-cache-staging bun run build:search && npx wrangler deploy --env staging && VERIFY_URL=https://sayit-hono-staging.audreyt.workers.dev/version DEPLOY_RETRY_HINT='CLOUDFLARE_ENV=staging vite build && npx wrangler deploy --env staging' bun run verify:deploy",
		"deploy:staging:assets": "bun run build:cache-version && CLOUDFLARE_ENV=staging vite build && npx wrangler deploy --env staging && VERIFY_URL=https://sayit-hono-staging.audreyt.workers.dev/version bun run verify:deploy",
		"dev": "vite dev",
		"dev:staging": "CLOUDFLARE_ENV=staging vite dev",
		"preflight:deploy": "tsx --tsconfig scripts/tsconfig.json scripts/preflight-deploy.ts",
		"verify:deploy": "tsx --tsconfig scripts/tsconfig.json scripts/verify-deploy.ts",
		"build:cache-version": "tsx --tsconfig scripts/tsconfig.json scripts/generate-cache-version.ts",
		"start": "bun run dev",
		"build": "vite build",
		"preview": "bun run build && vite preview",
		"audit-og-r2": "bun run scripts/audit-og-r2.ts",
		"build:search": "tsx --tsconfig scripts/tsconfig.json scripts/build-search-index.ts",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"test:watch": "vitest",
		"typecheck": "tsgo --noEmit",
		"lint": "oxlint src scripts vite.config.ts vite-plugin-sfc-ssr.ts",
		"fmt": "oxfmt --write src scripts test vite.config.ts vite-plugin-sfc-ssr.ts",
		"fmt:check": "oxfmt --check src scripts test vite.config.ts vite-plugin-sfc-ssr.ts",
		"check": "bun run lint && bun run fmt:check && bun run typecheck",
		"verify:lsc": "node scripts/verify-lsc.mjs",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vite-plugin": "^1.44.0",
		"@cloudflare/vitest-pool-workers": "^0.15",
		"@types/node": "^25",
		"@typescript/native-preview": "^7.0.0-dev.20260429.1",
		"@vitest/coverage-istanbul": "^4",
		"@vue/compiler-sfc": "^3.5.33",
		"fuse.js": "^7.3.0",
		"lemmascript": "0.5.14",
		"oxfmt": "^0.58.0",
		"oxlint": "^1.73.0",
		"tsx": "^4.21.0",
		"typescript": "^5.9.3",
		"vite": "^8.1.4",
		"vitest": "^4",
		"wrangler": "^4.107.0"
	},
	"dependencies": {
		"@resvg/resvg-wasm": "^2.6.2",
		"@vue/server-renderer": "^3.5.33",
		"cheerio": "^1.2.0",
		"hono": "^4.12.15",
		"marked": "^18",
		"satori": "^0.26.0",
		"vue": "^3.5.33"
	}
}
